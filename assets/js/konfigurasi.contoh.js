/*!
 * Client side settings. Copy this file to konfigurasi.js and fill it in.
 *
 * The Mapbox token is a public pk token, the kind Mapbox expects to appear in
 * page source. Restrict it by URL in the Mapbox console, account, tokens, URL
 * restrictions, so only hendrokuswantoro.com can spend the quota. Leave it
 * empty and the map quietly falls back to sources that need no key at all.
 */
window.HK_KONFIG = {
  mapboxToken: ""
};
